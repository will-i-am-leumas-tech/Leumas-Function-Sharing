I want to have a bulk feature for scanning numerous directories at once 

I want to have a remote feature for doing it with git repos and such. also in bulk

I want to have a feature so instead of always dropping the .leumas folder in the directory it ran, we also have the ability to put the .leumas folders into a desired output path dynamiccally controlled

indexing functions is nice but i would love for it to index our scripts as well so it will need to also scan to see if there are any callable js or py scripts and the path to call to run the file, maybe any props or arvs needed not just functions 

I want to have a simple CLI tool which will let us go through any of the .leumas folders it finds in our directory, and lets us run any function parsing props inside as needed. The mesh should work fast. basically cli tool for usage in our mesg. arrow keys and props given in terminal for quick use. 

feature to rerun index on all folders in mesh, so we dont have to do them all individually again.

